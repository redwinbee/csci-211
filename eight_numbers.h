#pragma once

class EightNumbers {
public:
    static void run();
    static bool ok(const int cross[], int cap);
    static void eightNumbers(int cross[], int cap);
    static void print(int cross[]);
};